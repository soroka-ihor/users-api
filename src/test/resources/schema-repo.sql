CREATE TABLE users (
    id VARCHAR(50) PRIMARY KEY,
    username VARCHAR(50),
    name VARCHAR(50),
    surname VARCHAR(50)
);

CREATE TABLE empty_users (
    id VARCHAR(50) PRIMARY KEY,
    username VARCHAR(50),
    name VARCHAR(50),
    surname VARCHAR(50)
);
